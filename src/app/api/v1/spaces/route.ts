import { notImplemented } from '@/lib/routeUtil';

export async function POST(_req: Request) {
  return notImplemented('POST /v1/spaces');
}

export async function GET(_req: Request) {
  return notImplemented('GET /v1/spaces');
}
